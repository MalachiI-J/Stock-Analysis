"""Experimental statistical forward-return prediction (Phase 5).

Separate from score_v1's deterministic, rule-based classification. Everything
here is a probabilistic estimate fit on historical technical indicators, not
a trading signal, and is clearly labeled as experimental wherever it is
surfaced (CLI output, reports, README).
"""
